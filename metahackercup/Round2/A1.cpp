#include <bits/stdc++.h>
using namespace std;

string solution(vector<int>& A, int N, int K) {

}

int main() {
   freopen("", "r", stdin);
   freopen("", "w", stdout);
   int T, N, K;
   cin >> T;
   for (int c = 1; c <= T; c++) {
        cin >> N >> K;
        vector<int> A(N, 0);
        for (int i = 0; i < N; i++) {
            cin >> A[i];
        }
        vector<int> B(N, 0);
        for (int i = 0; i < N; i++) {
            cin >> B[i];
        }
   } 
}